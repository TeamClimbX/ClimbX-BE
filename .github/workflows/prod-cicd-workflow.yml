name: Prod CI/CD Workflow

on:
  workflow_dispatch:

  push:
    branches: [ "main" ]

jobs:
  continuous-integration:
    name: Build and Push to ECR
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:9.3.0
        env:
          MYSQL_DATABASE: climbx
          MYSQL_USER: prod_user
          MYSQL_PASSWORD: prod_password
          MYSQL_ROOT_PASSWORD: root_password
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build with Gradle
        env:
          PROD_DB_URL: jdbc:mysql://localhost:3306/climbx?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
          PROD_DB_USER: prod_user
          PROD_DB_PASSWORD: prod_password
        run: ./gradlew build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to Amazon ECR
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "Building and pushing Docker image..."
          docker build -t ${{ secrets.PROD_BE_CORE_ECR_REPOSITORY_URI }}:$IMAGE_TAG .
          docker tag ${{ secrets.PROD_BE_CORE_ECR_REPOSITORY_URI }}:$IMAGE_TAG ${{ secrets.PROD_BE_CORE_ECR_REPOSITORY_URI }}:latest
          
          docker push ${{ secrets.PROD_BE_CORE_ECR_REPOSITORY_URI }}:latest
          docker push ${{ secrets.PROD_BE_CORE_ECR_REPOSITORY_URI }}:$IMAGE_TAG
          
          echo "✅ Image successfully pushed to ECR"

  continuous-deployment:
    name: Deploy Prod EC2
    runs-on: ubuntu-latest
    needs: continuous-integration
    if: success()

    steps:
      - name: Deploy to EC2 instance
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.PROD_BE_CORE_EC2_HOST }}
          username: ${{ secrets.PROD_BE_CORE_EC2_USERNAME }}
          key: ${{ secrets.PROD_BE_CORE_EC2_SSH_KEY_PEM }}
          port: 22
          script: |
            # 1. AWS ECR에 로그인
            # EC2에 연결된 IAM 역할 덕분에 Access Key 없이 로그인이 가능
            aws ecr get-login-password \
              --region ${{ vars.AWS_REGION }} \
            | docker login \
              --username AWS \
              --password-stdin ${{ secrets.PROD_BE_CORE_ECR_REPOSITORY_URI }}
            
            # 2. 최신 Docker 이미지 PULL
            docker pull ${{ secrets.PROD_BE_CORE_ECR_REPOSITORY_URI }}:latest
            
            # 3. 기존 컨테이너 강제 제거 (실행 중이거나 중지된 상태 관계없이)
            echo "Removing existing container if exists..."
            docker rm -f ${{ vars.PROD_BE_CORE_CONTAINER_NAME }} 2>/dev/null || echo "No existing container to remove."
            
            # 4. 새로운 컨테이너 실행
            echo "Starting new container..."
            docker run -d -p ${{ vars.SPRING_CONTAINER_PORT }}:${{ vars.SPRING_CONTAINER_PORT }} \
              --name '${{ vars.PROD_BE_CORE_CONTAINER_NAME }}' \
              -e PROD_DB_URL='${{ secrets.PROD_DB_URL }}' \
              -e PROD_DB_USER='${{ secrets.PROD_DB_USER }}' \
              -e PROD_DB_PASSWORD='${{ secrets.PROD_DB_PASSWORD }}' \
              -e KAKAO_NATIVE_APP_KEY='${{ secrets.KAKAO_NATIVE_APP_KEY }}' \
              -e GOOGLE_ANDROID_APP_KEY='${{ secrets.GOOGLE_ANDROID_APP_KEY }}' \
              -e GOOGLE_IOS_APP_KEY='${{ secrets.GOOGLE_IOS_APP_KEY }}' \
              -e APPLE_ANDROID_APP_KEY='${{ secrets.APPLE_ANDROID_APP_KEY }}' \
              -e APPLE_IOS_APP_KEY='${{ secrets.APPLE_IOS_APP_KEY }}' \
              -e JWT_SECRET='${{ secrets.JWT_SECRET }}' \
              -e AWS_S3_VIDEOS_SOURCE_BUCKET_NAME='${{ secrets.AWS_S3_VIDEOS_SOURCE_BUCKET_NAME }}' \
              -e AWS_S3_PROFILE_IMAGE_BUCKET_NAME='${{ secrets.AWS_S3_PROFILE_IMAGE_BUCKET_NAME }}' \
              -e AWS_S3_PRESIGNED_URL_EXPIRATION='${{ secrets.AWS_S3_PRESIGNED_URL_EXPIRATION }}' \
              -e AWS_S3_PROBLEM_IMAGE_BUCKET_NAME='${{ secrets.AWS_S3_PROBLEM_IMAGE_BUCKET_NAME }}' \
              -e AWS_S3_CLIMBING_GYM_IMAGE_BUCKET_NAME='${{ secrets.AWS_S3_CLIMBING_GYM_IMAGE_BUCKET_NAME }}' \
              -e AWS_CLOUDFRONT_DOMAIN='${{ secrets.AWS_CLOUDFRONT_DOMAIN }}' \
              -e AWS_S3_REGION='${{ vars.AWS_S3_REGION }}' \
              -e AWS_ACCESS_KEY_ID='${{ secrets.AWS_ACCESS_KEY_ID }}' \
              -e AWS_SECRET_ACCESS_KEY='${{ secrets.AWS_SECRET_ACCESS_KEY }}' \
              ${{ secrets.PROD_BE_CORE_ECR_REPOSITORY_URI }}:latest
            
            echo "New container started successfully."
            
            # 5. 사용되지 않는 오래된 Docker 이미지 정리 (디스크 공간 확보)
            docker image prune -af
            
            echo "🚀 Deployment to EC2 completed successfully!"